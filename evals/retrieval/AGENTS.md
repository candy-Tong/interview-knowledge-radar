# 检索质量评测

- `cases.json` 记录固定问题和 Top 2 的语义验收预期，不写死必须命中的文件名。
- `run.ts` 必须真实执行数据库候选召回、百炼 rerank 和独立 OpenAI-compatible LLM 裁判。
- 发送给裁判的知识内容必须是有界摘录；禁止打印密钥或整篇知识正文。
- 编排的确定性测试放在根目录 `tests/`，不得把 mock 测试混入本目录。
