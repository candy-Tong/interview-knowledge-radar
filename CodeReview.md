# Interview Knowledge Radar Code Review 规则

## 目标与适用范围

Code review 用于确认变更正确实现当前需求，不破坏产品约束，且具备与风险相匹配的验证证据。OpenCodeReview 负责稳定地选取变更文件和加载项目规则，审查者对最终判断负责。

满足任一条件即属于“大改动”，必须执行审查：

- 变更跨越多个模块或前端、服务端、数据库中的多个层级。
- 变更架构、数据契约、实时协议、数据库 schema、部署方式或重要产品交互。
- 影响安全、隐私、凭据、外部数据流、本地敏感日志或数据丢失风险。
- 引入、替换或升级运行时依赖，或改变构建、测试和发布链路。
- 即使只改一个文件，但失败后会中断核心面试流程或泄露敏感信息。

纯文字勘误、无语义格式化等低风险小改动可不执行 OpenCodeReview，但仍需人工检查 diff。

## 执行流程

1. 检查 `git --version`。OpenCodeReview 官方支持基线为 Git 2.41；更低版本即使部分命令可用，也不视为完整支持。
2. 首次使用 OCR 自身的 LLM 时，执行 `npx ocr config provider` 和 `npx ocr config model`；也可直接使用不需要 OCR LLM 凭据的 delegation 模式。
3. 确认审查边界：记录需求和验收条件，并固定一组后续命令都复用的 `<targetArgs>`：未提交工作区使用空参数，单个 commit 使用 `--commit <sha>`，分支变更使用 `--from <base> --to <head>`。工作区存在无关改动时，不得使用默认工作区模式。
4. 先完成与改动范围匹配的测试，至少执行 `npm test` 和 `npm run build`。通过命令不等于已通过审查。
5. 执行 `npm run review:preview -- <targetArgs>`，检查 OpenCodeReview 选取和排除的文件是否符合本次边界。
6. 已配置 OCR LLM 时执行 `npm run review -- <targetArgs> --background "<需求与验收条件>"`。凭据只保存在用户环境或 `~/.opencodereview/`，不写入仓库。
7. 未配置 OCR LLM 时使用官方 delegation 模式：执行 `npm run review:delegate -- <targetArgs>` 获取文件清单，再执行 `npm run review:rules -- <file1> <file2> ...` 获取适用规则，由当前编码 Agent 对这些文件完成审查。
8. 逐条处理发现：修复真实问题；对误报给出可核验的排除依据；对暂不处理的非阻断项记录风险和负责人。
9. 修复后重新执行受影响测试和 code review。在当前 GitHub Issue 中记录命令、结果、修复和剩余风险。

## 审查规则

### 需求与范围

- 变更必须完整满足 Issue 或需求中的验收条件，不得用更小、更容易验证但偏离目标的实现代替。
- 尽量写简洁的代码；如果没有特殊说明，不考虑需求之外的扩展，不做历史兼容。不为假设的未来需求预埋抽象、配置、分支或兼容层。
- 只评估本次变更引入或暴露的问题，不把无关的历史问题冒充为当前发现。

### 正确性与可靠性

- 检查主路径、边界条件、异步时序、取消与清理、重试、过期结果覆盖、重复事件和错误恢复。
- 数据契约、类型、API 语义、存储粒度和会话状态必须在前后端之间一致。
- 新增测试必须能在缺少实现或实现错误时失败，不得只断言 mock 自身的行为。

### 项目约束、安全与隐私

- 根目录及变更文件最近的 `AGENTS.md` 是权威约束；任何产品、数据流或目录规范冲突都属于缺陷。
- 不得请求麦克风或在系统音频共享失败时回退到麦克风；普通模式必须使用独立 ASR 上游。
- API Key、Authorization、数据库凭据和敏感日志不得进入前端包、响应、普通日志或 Git；外部数据流变更必须同步隐私说明。
- SQL 值使用参数化查询；WebSocket 保持路径白名单和 loopback Origin 校验；默认监听不得扩大到 `127.0.0.1` 之外。

### 可维护性与性能

- 优先清晰命名、短且单一职责的函数和直接数据流；删除无用分支、重复逻辑、空 catch 和无实际价值的中间层。
- 抽象必须由当前存在的重复或变化轴支持；不得以“以后可能需要”为理由增加接口、工厂、注册器或参数。
- 检查热路径中的重复请求、无界队列、过度渲染、未清理的定时器与侦听器，以及大文本的不必要拷贝。

### 验证与文档

- 验证要求与风险对应：除单元测试和构建外，按 `AGENTS.md` 执行数据库、检索、实时协议、浏览器布局或真实用户手势验证。
- 修改架构、数据契约、部署或关键产品约束时，检查最近的 `AGENTS.md`、`README.md` 和 `DEPLOYMENT.md` 是否同步。
- 审查不接受“理论上正确”作为验证；对每项验收条件给出可复现的命令、运行时行为或人工检查证据。

## 问题分级与通过标准

- `P0` 致命：凭据或敏感数据泄露、数据丢失、严重安全漏洞、核心流程完全不可用。
- `P1` 阻断：需求缺失或实现错误、产品硬约束破坏、主路径错误、明确竞态、无对应验证的高风险变更。
- `P2` 应修复：可维护性、次要边界、性能、测试质量或文档一致性问题。
- `P3` 建议：不影响正确性的局部改进；不得以个人偏好或未来扩展为由扩大范围。

审查意见必须包含文件与行号、可触发场景、实际影响和最小修复方向；无法证明的猜测不得作为缺陷。

通过条件：

- `P0` 和 `P1` 已全部解决，受影响的自动化与人工验证已重新通过。
- `P2` 已解决，或在 Issue 中记录了不处理理由、剩余风险和后续计划。
- 审查范围覆盖所有本次变更，没有用无发现、命令退出码或窄范围测试代替完成性证明。

## Issue 回写模板

```markdown
## Code review

- 范围：`<base>...<head>` 或工作区文件列表
- 需求背景：<issue / 验收条件>
- 命令：`npm run review:preview -- <targetArgs>`；`npm run review -- <targetArgs> --background "..."` 或 delegation 命令
- 结果：<P0/P1/P2/P3 数量与摘要>
- 处置：<已修复项和误报排除依据>
- 验证：<测试、构建、运行时或人工检查证据>
- 剩余风险：<无 / 具体风险与计划>
```
